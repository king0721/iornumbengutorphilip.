<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ngutor Philip | Systems Architect</title>
    <style>
        :root { --green: #1b5e20; --accent: #f9a825; --light: #f4f7f4; }
        body { font-family: 'Segoe UI', sans-serif; line-height: 1.6; color: #333; max-width: 1000px; margin: auto; padding: 20px; background: #fff; }
        header { display: flex; align-items: center; justify-content: space-between; border-bottom: 4px solid var(--green); padding: 20px 0; }
        .hero { display: grid; grid-template-columns: 1fr 1fr; gap: 40px; padding: 50px 0; align-items: center; }
        .hero-text h1 { font-size: 2.5em; color: var(--green); margin: 0; }
        .hero-img img { width: 100%; border-radius: 20px; box-shadow: 20px 20px 0 var(--green); }
        .badge { background: var(--green); color: white; padding: 5px 15px; border-radius: 50px; font-weight: bold; font-size: 0.9em; }
        .card-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px; margin-top: 40px; }
        .card { background: var(--light); padding: 30px; border-radius: 15px; border-top: 5px solid var(--accent); }
    </style>
</head>
<body>
    <header>
        <div>
            <h1>Ngutor Philip Iornumbe</h1>
            <p><strong>Senior Innovation Systems Architect</strong></p>
        </div>
        <img src="logo.png" alt="Trov Global" style="height: 50px;">
    </header>

    <section class="hero">
        <div class="hero-text">
            <span class="badge">TRL 4 Validated</span>
            <h2>BSF AIR: Decentralized Green Infrastructure</h2>
            <p>I architect modular, 100% solar-powered bioreactors that turn organic waste into high-value resources. Our IoT-enabled systems provide the verified data necessary for transparent international climate accounting.</p>
        </div>
        <div class="hero-img">
            <img src="profile.jpg" alt="Ngutor Philip">
        </div>
    </section>

    <div class="card-grid">
        <div class="card">
            <h3>The Technology</h3>
            <p>Off-grid, net-zero waste-to-value conversion using ML-driven yield simulation models.</p>
        </div>
        <div class="card">
            <h3>2026 Roadmap</h3>
            <p>Advancing from Lab Validated (TRL 4) to Field Pilots (TRL 5) across West Africa.</p>
        </div>
    </div>
</body>
</html>
